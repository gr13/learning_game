repos:
  - repo: local
    hooks:
      - id: pytest-coverage
        name: Run tests and show coverage
        entry: bash -c './env/bin/python -m coverage run -m pytest api/tests -q && ./env/bin/python -m coverage report --fail-under=80'
        language: system
        pass_filenames: false